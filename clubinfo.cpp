void Clubinfo(list<pair<int,string>> ls)
{
    cout<<"\t\t\t\t\tClubs Currently Running At DA-IICT\n\n";
    int i=1;
for (auto it = ls.begin(); it != ls.end(); ++it)
{
      if(it->second==" ")
      {
          
      }
      else
      {  cout <<i<<") "<< it->second <<"_Club"<< endl;
          i++;
      }
    
}
}
